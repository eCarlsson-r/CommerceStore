// components/Hero.tsx
export default async function Hero({ banners }: { banners: any[] }) {
  return (
    <div className="relative h-[500px] w-full overflow-hidden rounded-[3rem]">
      {/* Slider logic here using the banner.image_url */}
    </div>
  );
}
